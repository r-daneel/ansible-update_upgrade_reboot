---

# file: roles/update_upgrade_reboot/vars/main.yaml

update_upgrade_reboot_restart_required_file: '/var/run/reboot-required'

# restart message

update_upgrade_reboot_restart_message: "Reboot initiated by Ansible (role '{{ role_name }}')"
